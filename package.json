{
  "name": "aline-brand-workbench",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace @aline/web",
    "build:web": "npm run build --workspace @aline/web",
    "lint:web": "npm run lint --workspace @aline/web",
    "typecheck:web": "npm run typecheck --workspace @aline/web",
    "test:web": "npm run test --workspace @aline/web"
  },
  "engines": {
    "node": ">=22 <25"
  },
  "overrides": {
    "next": {
      "postcss": "8.5.10"
    }
  }
}
